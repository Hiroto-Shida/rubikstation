import { Layout } from "../../components/layout/Layout/container";
import { ReleasePagePresenter } from "./presenter";

export const ReleasePage = () => {
  return (
    <Layout>
      <ReleasePagePresenter />
    </Layout>
  );
};
